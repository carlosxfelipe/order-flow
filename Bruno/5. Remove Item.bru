meta {
  name: Remove Item
  type: http
  seq: 5
}

delete {
  url: http://localhost:5189/api/orders/{{orderId}}/items/{{itemId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{jwt_token}}
}
